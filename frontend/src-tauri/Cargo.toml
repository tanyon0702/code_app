[package]
name = "self-evolution-desk"
version = "0.1.0"
description = "Local chat and notes workspace"
authors = ["Codex"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[features]
default = []
